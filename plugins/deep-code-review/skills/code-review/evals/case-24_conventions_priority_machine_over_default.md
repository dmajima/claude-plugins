# case-24 規約優先順位解決（機械設定 > 言語デファクト）（conventions-resolution 2.2）

`.editorconfig`（優先度2: 機械設定）が言語プロファイルのデファクト規約（優先度5）と衝突する項目で、機械設定を「正」として評価し、デファクト違反を指摘しない分岐。5 段階優先順位解決の中核（上位が勝つ・項目単位オーバーライド）を検証する。信頼度整合の case-14 とは別の Step 2 規約源解決メカニズム。

## 入力

| 項目 | 内容 |
|-----|------|
| 起動フレーズ | "このブランチをレビューして"（差分: `src/Order/*.cs` 3 ファイル） |
| モード | 標準 |
| リポジトリ状態 | ルートに `.editorconfig` あり（`indent_style = tab` / プライベートフィールドを `dotnet_naming_rule` で `camelCase`（先頭アンダースコアなし）と規定）。`languages/csharp.md` のデファクトは スペース 4 インデント・`_camelCase` |

## 分岐の根拠

references/conventions-resolution.md セクション1（優先順位5段階・優先度2機械設定 > 優先度5デファクト・項目単位オーバーライド）・セクション2.1（指摘の根拠明記）・セクション2.2（デファクト規約と独自規約の衝突）・セクション3（適用規約サマリへの記録）・セクション4（禁止事項）、references/flow/flow.md Step 2（プロジェクト規約読込）、skill-rules-matrix.md C11 / C23 / U14。

## 期待動作

- Step 2: ルートの `.editorconfig`（優先度2: 機械設定）を読み込み、`languages/csharp.md` のデファクト規約（優先度5）と **項目単位で解決** する（conventions-resolution.md セクション1・全置換ではない）
- Step 2: `.editorconfig` がインデント（タブ）・命名（`dotnet_naming_rule`）を規定しているため、これらの項目は `.editorconfig` を「正」として評価し、csharp.md デファクト（スペース 4・`_camelCase`）を根拠にした指摘を出さない（セクション2.2）
- Step 2: 解決結果を「適用規約サマリ」に規約源（優先度）付きで記録し、Step 4 委譲引数 `language-profiles` / 適用規約サマリとして観点別スキルへ引き渡す（セクション3・C23）
- 統合時: 規約・スタイルに関する指摘には規約源を根拠として明記する（例: `.editorconfig` の `dotnet_naming_rule`〈優先度2: 機械設定〉。セクション2.1）
- `.editorconfig` が言及しない項目（例: XML doc コメントの有無）は下位の規約源（csharp.md デファクト・優先度5）で補完する（セクション1・項目単位オーバーライド）
- （以下は検出してはならない誤り）
    - `.editorconfig` を無視し csharp.md デファクトを根拠にタブインデントを「スペース推奨違反」と指摘する（優先度逆転・セクション4 禁止事項）
    - 機械設定（優先度2）と文書規約（優先度3）が矛盾する場合に文書規約を優先する（セクション1: 機械設定優先の違反）
    - 提出コード（レビュー対象差分）内のパターンを規約源として類推する（U14・セクション2.3 / 4 違反）

## 関連ケース

- case-06: 言語・FW 検出とプロファイル適用の正常系（規約源解決の前提）
- case-14: プロファイルアンカー照合による過小評価の是正（重要度整合の別分岐・Step 5。本ケースは Step 2 の規約源解決）
- case-25: 欠落系指摘の兄弟コード確認（同じ conventions-resolution.md の別セクション）
