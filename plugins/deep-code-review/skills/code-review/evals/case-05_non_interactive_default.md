# case-05 非対話モードでの既定動作（AskUserQuestion 不発火）

CI/CD・バッチ実行・SDK 経由等の非対話コンテキストでオーケストレーターが起動し、AskUserQuestion が使えないケース。モード選択のフォールバックと実行前通知を検証する。

## 入力

| 項目 | 内容 |
|-----|------|
| 起動フレーズ | "コードをレビューして"（スコープ引数なし・非対話コンテキスト） |
| モード | 非対話（AskUserQuestion 不可） |

## 分岐の根拠

`${CLAUDE_SKILL_DIR}/references/flow/mode-selection.md` セクション 2.3（非対話モード時の判定: AskUserQuestion 不可 → 標準モードで実行）、同「フォールバック時の実行前通知（必須）」、SKILL.md「実行モード判定」の「非対話モード時の既定: 標準モード」。

## 期待動作

- AskUserQuestion が利用不可のため、モード選択をスキップし **標準モードで実行** する（mode-selection.md セクション 2.3）
- レビュー実行前に「モード選択の確認ができなかったため標準モードで実行します」を 1 行で通知する（mode-selection.md フォールバック時の実行前通知）
- 集計セクションの「レビューモード」に「標準（フォールバック）」と記録する
- 非対話のため Agent Teams 採用判定もフォールバックし、サブエージェント方式で実行する（flow.md Step 3.5 のフォールバック条件「非対話モード」）
- inputs ヒアリングも AskUserQuestion 不可のためスキップし、その旨を制約に明記する

## 関連ケース

- case-04: 対話モードでのトリガー起動（モード確認あり）
- case-01: 標準モード初回レビューの詳細フロー
