---
title: {環境名（例: ローカル開発環境）}
sources:
  - {対応する設定ファイル等のグロブ（例: package.json / *.csproj / docker-compose.yml）}
related: []
updated: {YYYY-MM-DD}
---

# {環境名}

## 検証コマンド（変更後は必ず実行）

| 用途 | コマンド | 成功条件 |
|------|---------|---------|
| ビルド | `{build-command}` | {例: exit 0・警告なし} |
| テスト | `{test-command}` | {例: 全件 pass} |
| リント | `{lint-command}` | {例: エラー 0 件} |
| 型チェック | `{typecheck-command}` | {なければ行ごと削除} |

## 起動・動作確認

| 用途 | コマンド・手順 | 確認方法 |
|------|--------------|---------|
| ローカル起動 | `{run-command}` | {例: http://localhost:{port} が応答} |
| 停止 | `{stop-command}` | |

## 環境変数・設定

| 変数・設定 | 用途 | 設定方法 |
|-----------|------|---------|
| `{VAR_NAME}` | {用途} | {例: .env に記載（値はコミット禁止）} |

秘匿値（API キー・パスワード等）は **本ドキュメントに記載しない**。取得方法・保管場所のみ記す。

## デプロイ

デプロイを扱わない環境（ローカル開発環境等）では本節を削除する。

| 対象環境 | 手順・コマンド | 実行者・トリガー |
|---------|--------------|----------------|
| {例: ステージング} | {コマンド、または CI 定義ファイルへの参照} | {例: main へのマージで自動実行} |

## 環境構築手順（初回のみ）

1. {前提ツールのインストール（バージョン明記）}
2. {依存関係のインストールコマンド}
3. {初期データ・マイグレーション}

## デバッグ・トラブルシュート

| 症状 | 原因 | 対処 |
|------|------|------|
| {よくある症状} | {原因} | {対処} |

## 未確認事項

- TODO: {確認できなかった手順・コマンド}
