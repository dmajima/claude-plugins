---
title: {視点名（例: システム構成 / データモデル）}
sources:
  - {対応ソースのグロブ}
related: []
updated: {YYYY-MM-DD}
---

# {視点名}

## 全体像

```mermaid
flowchart TB
    subgraph {レイヤ・領域 1}
        A[{コンポーネント}]
    end
    subgraph {レイヤ・領域 2}
        B[{コンポーネント}]
    end
    A --> B
```

{図の補足説明（各コンポーネントの役割・責務境界）}

## モジュール・依存関係

| モジュール | 責務 | 依存先 | 配置 |
|-----------|------|-------|------|
| {モジュール名} | {責務} | {依存モジュール} | `{ソースパス}` |

## データモデル

```mermaid
erDiagram
    {ENTITY_A} ||--o{ {ENTITY_B} : "{関係}"
```

{主要エンティティの補足（ライフサイクル・制約）。データモデルを扱わない視点では本節を削除}

## 変更時の注意

- {依存方向のルール・触ってはいけない境界・拡張ポイント}

## 未確認事項

- TODO: {確認できなかった構成要素}
