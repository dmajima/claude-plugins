---
title: {画面・機能名}アクセス手順
sources:
  - {対応ソースのグロブ。未実装（status: draft / agreed）の場合はリスト全体を [] に置き換える}
related:
  - specs/{対応する仕様書}.md
status: {draft / agreed / implemented。spec-first（harness-define）生成時は draft。コード解析から生成した場合はこの行ごと削除}
updated: {YYYY-MM-DD}
---

# {画面・機能名}アクセス手順

{status が draft / agreed の場合のみ次行の注記を残し、implemented への昇格時に削除する。コード解析から生成した場合は注記ごと削除}
> **注記**: 本書は合意ベース（ユーザとの対話・提供資料に基づく記載）であり、実装による検証を経ていない。

## 画面の位置

| 項目 | 内容 |
|------|------|
| URL / ルート | `{URL パスまたはルート定義}` |
| ナビゲーション位置 | {例: グローバルメニュー > 設定 > ユーザ管理} |
| 必要権限 | {ロール・権限。不要なら「なし」} |
| 前提条件 | {例: ログイン済み・特定データの存在} |

## アクセス手順

1. {起点（例: トップページ）} を開く
2. {操作（例: メニュー「設定」をクリック）}
3. {到達確認（例: ユーザ一覧テーブルが表示される）}

## 画面内の主要導線

```mermaid
flowchart LR
    A[{本画面}] -->|{操作}| B[{遷移先 1}]
    A -->|{操作}| C[{遷移先 2}]
```

{図の補足説明（各導線の用途・利用条件）。図を解釈しない読者にも内容が伝わるよう文章でも記す}

## 未確認事項

- TODO: {確認できなかった導線・条件}
