# {skill-name} の依存パッケージリスト（テンプレート）
#
# このファイルは Python 利用スキルのテンプレート。利用するスキルが
# このテンプレートをコピー後、必要な依存パッケージを列挙する。
# venv の構築・撤去は environment-setup-toolkit に委譲する。
#
# 形式（推奨）:
#   - バージョン固定: `package_name==1.2.3`
#   - 互換範囲: `package_name>=1.0,<2.0`
#   - 最新許容: `package_name`
#
# 例:
# requests==2.31.0
# Pillow>=10.0,<11.0
