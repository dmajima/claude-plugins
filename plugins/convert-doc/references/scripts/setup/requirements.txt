# 依存パッケージ（完全固定: pip install で同一バージョンを強制）
#
# 【CVE 監視ポリシー】L-11 / extension-review security 指摘
# - 定期的に `pip-audit` または `osv-scanner` を実行し、各パッケージの CVE 公開状況を確認すること
# - 特に lxml (CVE-2021-43818 / CVE-2022-2309 等の履歴あり) と Pillow (画像処理系で頻繁に CVE 公表) は要監視
# - python-pptx 内部の lxml は直接ハードニングできないため、XML 攻撃対策は
#   convert_from_pptx.py 側の ZIP bomb 検査と上限定数（MAX_SLIDES 等）で行う
#   （かつての defusedxml.lxml.monkey_patch_lxml() は defusedxml 0.7 で API 削除済み）
# - バージョン上げ時は pip install で動作確認のうえ pin 更新する

markdown==3.7
Pygments==2.18.0
rcssmin==1.1.2
rjsmin==1.2.2
Pillow==11.2.1
playwright==1.48.0
python-pptx==1.0.2
requests==2.32.3
lxml==5.3.0
