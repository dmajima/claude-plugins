# projectboard スキルは Python 標準ライブラリのみで完結し、外部 PyPI 依存はゼロ（ADR-2/ADR-3）
# urlkey.py / tasks_to_csv.py / analyze_schedule.py が使用するのは sys / re / json / csv / datetime / argparse のみ。
# 本ファイルは python-venv ルール準拠のため空のまま配置する（インストール対象なし）。
