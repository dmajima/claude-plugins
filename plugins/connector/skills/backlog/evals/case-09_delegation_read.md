# Case 09: 他プラグイン委譲による課題取得（パターン B・読み取り）

## 入力

| 項目 | 値 |
|-----|---|
| 起動フレーズ | `Skill(skill: "connector:backlog", args: "読み取りのみ。PROJ-123 の件名・本文・コメントを取得して")` |
| 引数 | 課題キー + 「読み取りのみ」 |
| フラグ | なし |
| 既存状態 | 呼び出し元は investigation プラグイン。credentials.json に Backlog API キー登録済み。**後続フローなし（取得結果の報告で呼び出し元のターンが完了する文脈）** — 後続フローがある場合は `Skill()` ではなく `Agent()` を使う（delegation-interface.md セクション 3） |

## 期待動作

1. パターン B（読み取りのみ）と判別
2. 認証確認（API キーの存在確認）
3. 課題 + コメントを API で取得
4. 結果を **解釈・要約せずそのまま** 呼び出し元に返す
5. 書き込み操作（コメント投稿・ステータス更新）には進まない

## 期待出力

| 項目 | 期待値 |
|-----|-------|
| 生成ファイル | なし |
| 標準出力（要約） | 課題メタ情報 + コメント一覧を透過返却（外部由来データ境界マーカー付き） |
| 終了状態 | 成功 |

## 分岐の根拠

パターン B の読み取り。「読み取りのみ」キーワードにより書き込み操作に誤って進まないことを確認する。

## 関連ケース

- `case-01_issue_get.md`（パターン A のユーザー直接取得）
